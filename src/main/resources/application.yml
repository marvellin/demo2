
server:
  port: 8081
  servlet:
    context-path: /api/

spring:
  application:
    name: demo2
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/demo2?useSSL=false&serverTimezone=UTC&characterEncoding=utf-8